const enum PAGE_TYPES {
    MAIN = 'main',
    SEARCH = 'search'
}

enum PAGE_TITLES {
    MAIN_PAGE_TITLE = 'Тува Медиа Групп — Новости Тувы',
    SEARCH_PAGE_TITLE = 'Поиск последних новостей Тувы'
}

const enum CSS_PROPS {
    DISPLAY = 'display',
    OPACITY = 'opacity'
}

const enum ELEMENT_ATTRIBUTES {
    BVI_PANEL_HIDE = 'data-bvi-panel-hide'
}

const enum COOKIES {
    COOKIE_BLOCK = 'cookie-block-closed'
}

const enum METHOD {
    GET = 'GET',
    POST = 'POST',
    PUT = 'PUT',
    DELETE = 'DELETE'
}

export { PAGE_TYPES, PAGE_TITLES, CSS_PROPS, ELEMENT_ATTRIBUTES, COOKIES, METHOD}